# requirements_docker.txt
# Inference-only requirements for MiRACLE Docker container.
# CPU-only PyTorch for maximum portability (no CUDA driver needed on host).
# Generated from: imports used by src/inference/ + src/rl/rl_meta_controller.py
#
# To build: docker build -t miracle-inference .
# To run:   docker run miracle-inference --help

# ── Core scientific stack ────────────────────────────────────────────────────
numpy>=1.26.0,<3.0
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0

# ── PyTorch (CPU-only, keeps image small — ~800MB vs ~4GB for CUDA) ─────────
# On CPU the 30-day inference takes ~30s, acceptable for demo/interview use.
# For GPU inference, replace with: torch==2.4.0+cu121 from pytorch index.
torch>=2.2.0
torchmetrics>=1.3.0

# ── Lightning ecosystem (required by pytorch-forecasting / TFT) ─────────────
pytorch-lightning>=2.2.0
lightning>=2.2.0

# ── TFT model (pytorch-forecasting wraps the TFT architecture) ──────────────
pytorch-forecasting>=1.0.0

# ── PV / solar physics ───────────────────────────────────────────────────────
pvlib>=0.11.0

# ── Weather API client (OpenMeteo — free, no API key needed) ─────────────────
openmeteo-requests>=1.2.0
requests-cache>=1.1.0
retry>=0.9.2
requests>=2.32.0
urllib3>=2.0.0

# ── Data I/O ─────────────────────────────────────────────────────────────────
pyarrow>=14.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
python-dotenv>=1.0.0
